spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/odontosuite
    username: postgres
    password: odontosuite
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://TU_ISSUER/

server:
  port: 8082